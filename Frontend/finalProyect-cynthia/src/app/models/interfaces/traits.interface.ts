export interface TraitResponse {
  id: number;
  name: string;
  description: string;
  avatar: string;
}

export interface CreateTraitResponse {
  id: number;
  name: string;
  description: string;
}
